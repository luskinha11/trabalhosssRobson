var nome = "Lucas"
var idade = 21;
var estaSolteiro = true;

// if (estaSolteiro) { // retorno da decisão true
if (estaSolteiro === true) { // retorno da decisão true
    console.log(nome + " está solteiro!");
  } else {
    console.log(nome + " está casado!");
  }



  /*

  Expressões IF e ELSE

  Chamamos de expressão if e else
  que são o que nos habilita a tornarmos decisões dentro de nosso
  codigo.

  Veja o exemplo, declaramos algumas variáveis e criamos um
  trecho de expressão na linha 7, mas caso o retorno seja negativo irremos
  executar a segunda expressão na linha 9.
  
  O que se encontra entre parenteses apos o if nós chamaos de
  decisão lógica, pois será baseado nesta decisão que iremos controlar
  qual expressão será logicamente executada, fazendo assim o controle da estrutura.

  */